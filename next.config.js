/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['goggles.su', 'images.unsplash.com', 'unsplash.com'],
  },
}

module.exports = nextConfig
